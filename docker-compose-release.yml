version: '3.4'

services:
  concerto_server:
    image: etav/concerto_server
    build:
      context: .
      dockerfile: Server/Dockerfile
      
  concerto_reverseproxy:
    image: etav/concerto_reverseproxy
    build:
      context: ./nginx
      dockerfile: ./Dockerfile